<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.david.edison">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".startup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".studentStartup" />
        <activity android:name=".adminStartup" />
        <activity android:name=".teacherStartup" />
        <activity android:name=".student" />
        <activity android:name=".subject" />
        <activity android:name=".teacher" />
        <activity android:name=".room" />
        <activity android:name=".exam" />
        <activity android:name=".examResult"></activity>
    </application>

</manifest>